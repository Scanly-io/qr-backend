# Docker Environment Variables
# This file contains shared configuration for all services running in Docker
# Copy this to .env.docker.local and customize for your environment

# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379

# Kafka/Redpanda Configuration
KAFKA_BROKER=redpanda:9092

# JWT Configuration (CHANGE IN PRODUCTION!)
JWT_SECRET=docker-dev-secret-change-in-production
JWT_EXPIRES_IN=15m

# Service Ports (internal container ports)
AUTH_SERVICE_PORT=3001
QR_SERVICE_PORT=3002
ANALYTICS_SERVICE_PORT=3004
MICROSITE_SERVICE_PORT=3005
