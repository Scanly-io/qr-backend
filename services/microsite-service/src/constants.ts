/**
 * Cache version for microsite HTML
 * 
 * Increment this version number whenever you update the rendering code
 * to automatically invalidate all old cached HTML and force regeneration.
 * 
 * Examples of when to bump:
 * - Theme color system changes
 * - New block features or styling updates
 * - Bug fixes in HTML generation
 * - Responsive design improvements
 */
export const CACHE_VERSION = "v1";
